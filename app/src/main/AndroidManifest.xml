<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thoughtworks.androidtrain">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Helloworld">
        <activity android:name=".activities.ConstraintActivity" />
        <activity android:name=".activities.FragmentDemoActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RecyclerViewActivity" />
        <activity android:name=".activities.TimerActivity" />
        <activity android:name=".activities.HandlerActivity" />
        <activity android:name=".activities.RxJavaActivity" />
        <activity android:name=".activities.SPPromptActivity" />
        <activity android:name=".activities.SPMainActivity" />
        <activity android:name=".activities.MomentsActivity" />
    </application>

</manifest>